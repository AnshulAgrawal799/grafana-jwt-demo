# Backend settings for running the JWT server outside Docker.
# Copy this file to backend/.env if you run the backend manually.

PORT=4000
ALLOWED_ORIGINS=http://localhost:8080
AUTHORIZED_EMAIL_DOMAINS=wheelocity.local,demo.local,test.com
MAX_TOKEN_TTL_MINUTES=60
GRAFANA_URL=http://localhost:3000